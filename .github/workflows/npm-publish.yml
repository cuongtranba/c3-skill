name: Publish npm client

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - name: Use the release workflow
        run: |
          echo "npm publishing is handled by .github/workflows/release.yml."
          echo "That workflow reads the package name from packages/cli/package.json and authenticates with the NPM_TOKEN secret."
